{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:deploy": "npm run docs:build && gh-pages -d docs/.vitepress/dist -b gh-pages"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "@types/node": "^22.14.1",
    "fs": "^0.0.1-security",
    "gh-pages": "^6.3.0",
    "path": "^0.12.7"
  }
}
